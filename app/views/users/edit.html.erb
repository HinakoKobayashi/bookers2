<h2>User info</h2>

<%= form_with model: @user do |f| %>

  <%= f.file_field :image, accept: "image/*" %>
  <h4>name</h4>
  <%= f.text_field :name %>
  <h4>introduction</h4>
  <%= f.text_area :introduction %>
  <%= f.submit '編集' %>

<% end %>